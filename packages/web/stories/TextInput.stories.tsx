import { TextInput } from '@elemental-zcash/components';

export default {
  component: TextInput,
  title: 'Components/TextInput',
  tags: ['autodocs'],
};

export const Default = {
  args: {
  },
};
